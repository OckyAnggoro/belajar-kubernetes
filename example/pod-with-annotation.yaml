apiVersion: v1
kind: Pod
metadata:
  name: nginx-with-annotation
  labels:
    environment: production
  annotations:
    description: Deploy applikasi dengan annotationk
spec:
  containers:
    - name: nginx
      image: nginx
      ports:
        - containerPort: 80